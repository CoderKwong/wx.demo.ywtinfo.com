(function (a) {
    hzAPI.loginSuccess();
    a("A.login_submit").bind("tap", function () {
        if (validate.check("#login form")) {
            login.android_Fx_uuid()
        }
    });
    a("a.logout_submit").bind("tap", function () {
        hzAPI.logout()
    })
})(Zepto);